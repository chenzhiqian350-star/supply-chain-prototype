# 沟通记录 · 产品 SKU 主数据

## 需求确认（已确认决策）
- **单一结构化弹窗维护全部 27 个字段**，分四组：① 基本信息（8）② 客户与物流（10）③ 周期参数-天（5）④ 产品 CBM 上限（SKU × 柜型，4）。
- **字段来源显式标注**：从 SAP 获取 / 人手维护 / 产品主数据 / 排程系统；便于判断字段异常时的维护入口与责任方。
- **产品 CBM 上限（SKU × 柜型）**（40'RF / 40'HQ / 20'RF / 20'GP）按 SKU × 柜型 分别维护在 SKU 主数据；货柜校验取柜内所有 SKU 上限的 MIN。
- **保存校验**：产品编码必填且唯一；缺少件装量、目的地或周期的 SKU 允许保存但**不参与自动预排**并提示。
- **有效业务组合**：SKU + 客户 + 目的地三者缺一不可参与预测与预排；新增预测行只带出启用且组合有效的记录。
- **启用 / 停用**：停用后不参与后续预排，**保留历史计划与操作日志**；非 GMP SKU 停用需二次确认并写入日志。
- **变更不追溯**：主数据变化**不自动改写未执行货柜**；目的地或默认运输方式变更影响未执行计划时生成「主数据变更影响」预警，由 SCM 复核。
- **批量导入**：作为辅助手段，不能直接覆盖已排计划。

## 交付范围（本次产出）
- 交互原型：复用现有系统 `supply-chain-prototype.html#skuMaster`（已复制到 `原型/`，不做独立原型）
- 业务流程图：`流程图/SKU主数据-flow.html`（纯 HTML/CSS，零 CDN 依赖；含保存校验分支、变更影响判定分支、启用停用分支）
- PRD：`SKU主数据/需求文档/SKU主数据-PRD.html`（左正文右活原型双栏，12 章节 + 27 字段清单四组专章 + 字段口径与计算规则）
- 真实截图 2 张：SKU 主数据列表（含字段来源标注）、规则说明卡（产品 CBM 上限与字段来源说明）

## 字段口径引用
- 箱数 = `ceil(产品数量 / 件装量)`；批次 CBM = `箱数 × 每件 CBM`。
- 日期链：最晚 QC → 计划装柜 → ETD → ETA → T0；检验 / 装柜按香港工作天，离港 / 运输按自然天，T0 按内地工作天。
- 参与自动预排前置条件：启用状态 + 件装量 + 目的地 + 周期参数四项完整。

## 产物路径
`SKU主数据/` 目录，沿用 edu-pm-workflow 技能标准产出，双栏右侧沿用现有系统页面。

## 下一步 / 待确认
- 二期项：批量导入增强（变更预览与回滚）、字段级变更历史、按客户/目的地的组合有效性看板。
- 完整预览（右侧活原型、保存、带图复制）需先运行 `python3 scripts/start_service.py --install`（Playwright + Chromium）再 `--serve`。
- 上线计划时间待确认。

## V1.1 补充确认（2026-09-20）
- **工具栏新增「同步 SAP」**：一键从 SAP 产品主数据接口只读拉取最新字段值，仅覆盖「从 SAP 获取」类字段，**人手维护字段不被覆盖**；同步不追溯改写既有货柜，涉及未执行计划的变更仍按原规则生成「主数据变更影响」预警。原型为静态占位，功能由开发实现。
- **工具栏新增「下载模板」**：固定置于「导入主数据」左侧；下载标准 CSV 模板（文件名「SKU主数据导入模板.csv」，27 字段中文表头，带 UTF-8 BOM），供离线填报后原样导入。
- **查询区新增「SPU」条件**：与产品编码、目的地、运输方式为 AND 叠加关系，支持模糊匹配、大小写不敏感。
- 埋点新增 `sku_sync_sap`、`sku_template_download`；上线计划一期同步纳入三项。
- 交付物同步：`需求文档/body.html` 升版 V1.1、`需求文档/SKU主数据-PRD.html` 重新注入、`原型/supply-chain-prototype.html` 与主原型统一基线。
